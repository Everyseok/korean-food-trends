# Cloudflare Workers environment variables
# Set these in your wrangler.jsonc [vars] section or via Cloudflare Dashboard

ADMIN_SECRET=your-strong-secret-here

# For local development with wrangler dev:
# DATABASE_URL is not used — D1 binding is used instead
